export type User = {
  username: string;
  balance: number;
  notifications: number;
  bitcoinAmount: number;
};
export type Weapon = {
  name: string;
  image: string;
  price: number;
  availability: number;
};

export type Activity = {
  name: string;
  image: string;
  hot: boolean;
};

export type Case = {
  name: string;
  image: string;
  price: number;
  availability: number;
};
